import { ResultsClient } from "@/components/site";
export default async function Page({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) { return <main><div className="page-header"><div className="container"><div className="eyebrow" style={{ color: "var(--teal)" }}>FIND YOUR WAY</div><h1 className="display">Cruises worth making time for.</h1><p>Search the full depth of cruise possibilities, then let the details make the decision feel simple.</p></div></div><div className="container"><ResultsClient searchParams={await searchParams} /></div></main>; }
